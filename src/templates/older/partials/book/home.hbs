{{>bookSubnav}}

<div class="container-fluid">
  <!-- two rows: row-cols-md-2 -->
  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3">
    {{#each (reverse content.all)}}
      <div class="col mb-4">
        {{>poemPrimary}}
      </div>
    {{/each}}
  </div>
</div>
