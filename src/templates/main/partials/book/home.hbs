{{>bookNav}}

<div class="container-fluid">
  <!-- two rows: row-cols-md-2 -->
  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3">
    {{#each content.latest}}
      <div class="col mb-4">
        {{>cardPoem}}
      </div>
    {{/each}}
  </div>
</div>
